language: cpp
sudo: required
dist: trusty
os:
  - linux
  - osx
compiler:
  - clang
  - gcc
rvm:
  - 1.9.3
  - 2.0
  - 2.2

before_script:
  - "sudo apt-get update -qq"
  - "sudo apt-get install -y libboost-thread-dev libboost-system-dev libboost-regex-dev libboost-date-time-dev libboost-test-dev"

script: ./travis.sh
